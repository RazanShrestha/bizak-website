================================================================================
  BIZAK PAGE GENERATION / REDESIGN PROMPT
  Placeholders:  [REFERENCE_FILE]  |  [TARGET_FILE]  |  [PAGE_CATEGORY]
================================================================================

You are a senior React developer and product designer working on a B2B SaaS /
ERP application called Bizak.

─── YOUR TASK ───────────────────────────────────────────────────────────────────

Read the reference page at [REFERENCE_FILE] in full.
Your target is [TARGET_FILE].

If the target file is empty or a stub  →  generate it from scratch.
If the target file already has content →  redesign it so it follows the design language as the reference.

In both cases the output must feel like a natural part of the same product while
being different and distinctly shaped around the subject matter of [TARGET_FILE].

─── THE DESIGN LANGUAGE (non-negotiable) ────────────────────────────────────────

1.  CSS CLASS SYSTEM
    All layout, typography, and component classes come from
    src/styles/style.css via the biz-* namespace.  Read the reference file
    to understand which classes map to which visual patterns.
    Use the same classes.  Do not invent new class names.
    Supplement with inline styles only when the class system leaves a gap.

2.  SECTION STRUCTURE

    You decide the order best suited for the page where we are working on.
    You decide which sections get more visual
    weight.  The reference uses a particular order you are free to change it
    if a different hierarchy serves [TARGET_FILE] better.
    You can get creative with the design too , it doesnot necessarily have to be the same design but better.(emphasis on be creative)
    (Be creative with layout, visual hierarchy, and how sections relate spatially. 
    All components in the page are required but their arrangement and presentation style are entirely up to you.
    Consider what the user's primary action is and design around that but its upto you to decide whats the best choice is.)

3.  COLOR & ANIMATION TOKENS
    Primary accent:   #C7FF35  (neon lime)
    Olive / muted:    #7A826D
    Deep background:  var(--biz-deep)
    Pulse animation:  biz-pulse-glow
    Flow animation:   biz-flow  (dashed animated SVG path)
    Particle dots:    biz-data-dot  biz-particle
    Neon border card: biz-neon-border

4.  SVG ICONS
    Define a local  Icon({ name, size, style })  helper at the top of the
    file with inline SVG paths no external icon library.  Keep icons
    stroke-based (fill="none", stroke="currentColor", strokeWidth≈1.8).
    Choose icons that are semantically correct for the subject matter.


─── TECHNICAL REQUIREMENTS ──────────────────────────────────────────────────────

  • File:          [TARGET_FILE]
  • Imports:       import "../../styles/style.css"
                   import { Header } from "./Header"
                   import { Footer } from "./Footer"
  • No external dependencies beyond what already exists in the project.
  • All SVG icons defined inline in the file.
  • The page component wraps everything:
      <div className="biz-page" style={{ fontFamily: "'Inter', sans-serif" }}>
        <Header />
        <main style={{ paddingTop: 76 }}>  ...sections...  </main>
        <Footer />
      </div>
  • Each section is its own named function component defined in the file.
  • No TypeScript errors.  Run a mental type-check before finalising.
  • No comments explaining what the code does only write a comment if
    a non-obvious constraint or workaround is present.
  • Do not add features or abstractions beyond what the page requires.

─── BEFORE YOU WRITE A SINGLE LINE ─────────────────────────────────────────────

1.  Read  [REFERENCE_FILE]  completely understand every section, every
    CSS class, every animation pattern, every card composition to understand the design language.
2.  Read  src/styles/style.css  (at minimum the biz-* sections) so you know
    exactly which class names are available and what they do visually.
3.  Read  src/app/routes.tsx  to confirm the export name and route path.
4.  Then design think about the page's primary user action, the most
    compelling visual for the hero, and the section order that builds the
    strongest narrative before writing any JSX.

================================================================================
