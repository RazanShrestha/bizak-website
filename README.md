
  # Convert Home Page to HTML CSS

  This is a code bundle for Convert Home Page to HTML CSS. The original project is available at https://www.figma.com/design/G2gczWY1VirxICB9zQ6kCA/Convert-Home-Page-to-HTML-CSS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  